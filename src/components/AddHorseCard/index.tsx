import styles from "./addHorseCard.module.css";

function AddHorseCard() {
  return (
    <div
      className={styles.container}
      onClick={() => alert("Not yet implemented")}
    >
      +
    </div>
  );
}

export default AddHorseCard;
